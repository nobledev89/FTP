# FinTechPulse environment template
#
# Copy this file to .env.local for the web app; never commit real values.
# The worker has a separate template at local-worker/.env.example.
# Validate a filled file with `pnpm env:check:web`.
# In Vercel, add these values in Project Settings -> Environment Variables.
#
# Local Supabase values are printed by `pnpm supabase:start`.

# ---------------------------------------------------------------------------
# Web / Vercel
# ---------------------------------------------------------------------------

# Canonical public origin, no trailing slash. Used for canonical URLs, sitemap, RSS, and OG tags.
# Production (Vercel): https://fintechpulse.co.uk. Local development: http://localhost:3000.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Browser-safe Supabase settings.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=replace-with-supabase-publishable-key

# Shared secret for the signed /api/revalidate endpoint. At least 32 random characters.
# Generate one with: pnpm node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
REVALIDATION_SECRET=replace-with-at-least-32-random-characters

# Contact form delivery (server-only; never prefix with NEXT_PUBLIC_). Messages from /contact are
# emailed through Resend (https://resend.com) to CONTACT_TO_EMAIL, which is never shown on the site.
# Without these two values the form stays up but tells visitors it is temporarily unavailable.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
# Optional. Defaults to Resend's test sender, which can only deliver to the Resend account's own
# address. After verifying fintechpulse.co.uk in Resend, use e.g. FinTechPulse <contact@fintechpulse.co.uk>.
CONTACT_FROM_EMAIL=

# The web app does NOT use the Supabase service-role key. Do not add it to Vercel.
